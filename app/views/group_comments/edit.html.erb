<div class="container">
	<div class="row">
		<div class="col-lg-1">
		</div>

		<div class="col-lg-9 diary_title">
			<h2 class="post_image1">日記編集</h2><br>
				<%= form_with url: group_event_group_comment_path(@group_comment.id), method: :patch  do |f| %>
				<%= f.text_area :group_comment, placeholder:"400字以内で入力してください", style: "width: 500px; height: 250px; font-size: 20px;", value: @group_comment.group_comment %><br/>
					<%= f.submit "編集完了", class: "btn btn-primary", style: "width: 120px; font-size:20px;" %>
					<%= link_to "削除", group_event_group_comment_path(@group_comment.group_event_id), method: :delete, "data-confirm" => "本当に削除しますか", class: "btn btn-danger", style: "width: 120px; font-size:20px;" %>
				<% end %>
		</div>
	</div>
</div>