<h1>GroupPhoto#new</h1>

<div class="container">
	<div class="row">
		<div class="col-lg-1">
		</div>

		<div class="col-lg-9">
			<h2>写真を投稿する</h2><br>
				<%= form_with url: group_event_group_photos_path(@group_event.id) do |f| %>
					<laber>写真</laber><%= f.file_field :group_photo_image_id %>
					<%= f.submit "編集完了", class: "btn btn-primary"%>
				<% end %>
		</div>
	</div>
</div>