<div class="container">
	<div class="row">
		<div class="col-lg-1">
		</div>

		<div class="col-lg-9" >
				<%= form_for(@event) do |f| %>
					<laber class="edit_event_laber">イベント名</laber><%= f.text_field :event_title, class: "form5", placeholder: "イベント名を入力してください", style: "width: 300px;" %></br>
					<%= f.submit "編集", class: "form6 btn btn-primary"%>
				<% end %>
		</div>
	</div>
</div>
