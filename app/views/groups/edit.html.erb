<div class="container">
	<div class="row">
		<div class="col-lg-1">
		</div>

		<div class="col-lg-9">
			<h2>編集画面</h2><br>
				<%= form_for(@group) do |f| %>
					<label>カレンダー名</label><%= f.text_field :group_name %><br/>

					<%= f.submit "編集完了", class: "btn btn-primary"%>
				<% end %>
		</div>
	</div>
</div>