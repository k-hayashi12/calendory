<div class="container">
	<div class="row">
		<div class="col-lg-1">
		</div>

		<div class="col-lg-9">
			<h2>編集画面</h2><br>
				<%= form_for(@group_event) do |f| %>
					<laber>イベント名</laber><%= f.text_field :group_event_title %>
					<%= f.submit "編集完了", class: "btn btn-primary"%>
				<% end %>
		</div>
	</div>
	<%= link_to '戻る', :back, class: "btn btn-default" %>
</div>