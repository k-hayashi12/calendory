<% @event.photos.each do |photo| %>
	<%= image_tag photo.photo_image_id.to_s, class: "album_image" %>
	<%= link_to "削除", photo_destroy_path(photo), method: :delete, "data-confirm" => "本当に削除しますか？" %>
<% end %>